document.addEventListener('DOMContentLoaded', function() {
    AOS.init();
});

  /**
   * Initiate glightbox
   */
  const glightbox = GLightbox({
    selector: '.glightbox'
  });

  
  $('#myModal').modal(options)
